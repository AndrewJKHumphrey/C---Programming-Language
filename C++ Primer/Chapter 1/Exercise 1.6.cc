/* #include <iostream>

int main() 
{

int v1 = 2, v2 = 2;

//The ; at the end of each line breaks the std::cout statment and thus makes each line break
//Removing the two inner ; will make it one big line again
std::cout << "The sum of " << v1//;
<< " and " << v2//;
<< " is " << v1 + v2 << std::endl;

return 0;
} */