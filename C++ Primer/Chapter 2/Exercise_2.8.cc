#include <iostream>

int main()
{
    //cise 2.8: Using escape sequences, write a program to print 2M followed by a newline. 
    //Modify the program to print 2, then a tab, then an M, followed by a newline.

    std::cout << "\62 \t \115 \n" << std::endl;
    return 0;
}