//int minimum size is 16 bits, which will be at as large as a short

//long minimum size is 32 bits, which will be as large as an int

//long long minimum size is 64 bits, which will be as largas an long, new standard

//short minmimum size is 16 bits


//unsigned values that are positive only 0 to 255

//signed values than can be negative -127/-128 to 127


//float single precision floating point six significant digits

//double double precision floating point 10 significant digits

//long double edxtended precision floating point 10 significant digits